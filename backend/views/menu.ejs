<!-- Restaurant Menu -->
<section class="section fade-in" id="menu">
  <div class="container">
    <a href="/restaurants" class="btn btn-outline-primary mb-4">← Back to Restaurants</a>

    <h2 class="section-title text-center mb-4"><%= restaurant.name %></h2>

    <div class="row g-4">
      <% if (menuItems.length > 0) { %>
        <% menuItems.forEach(item => { %>
          <div class="col-md-4 col-sm-6">
            <div class="menu-item text-center">
              <img src="/images/<%= item.image %>" alt="<%= item.name %>">
              <h6 class="mt-3"><%= item.name %></h6>
              <p class="text-muted mb-2">$<%= item.price %></p>
              <p class="menu-description"><%= item.description %></p>
              <button class="btn btn-primary btn-sm" 
                onclick="addToCart('<%= item.name %>', '<%= item.price %>', '<%- item.image %>')">
                Add to Cart
              </button>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <p class="text-center text-muted">No menu items available.</p>
      <% } %>
    </div>
  </div>
</section>
